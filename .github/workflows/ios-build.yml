name: Build iOS TrollStore IPA

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  # Xác định biến môi trường PYTHON nếu node-gyp cần
  PYTHON: python3

jobs:
  build:
    name: Build iOS
    runs-on: macos-latest
    defaults:
      run:
        working-directory: .

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Native Build Tools
        run: |
          npm install -g node-gyp
          # Bỏ lệnh 'npm config set python python3' vì npm v9+ không còn hỗ trợ cấu hình này.

      - name: Generate Package Lock
        run: npm install --package-lock-only --legacy-peer-deps

      - name: Setup Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install Node Modules
        run: npm install --legacy-peer-deps

      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: ios/Pods
          key: ${{ runner.os }}-pods-${{ hashFiles('ios/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: Install CocoaPods
        run: |
          cd ios
          pod install --repo-update
        
      - name: Build App Bundle (Unsigned)
        run: |
          # Đảm bảo không có lỗi xcpretty bằng cách bắt lỗi exit của xcodebuild
          set -o pipefail
          xcodebuild -workspace ios/MeoPhim.xcworkspace \
            -scheme MeoPhim \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY="" \
            AD_HOC_CODE_SIGNING_ALLOWED=YES \
            IPHONEOS_DEPLOYMENT_TARGET=16.0 \
            clean build | xcpretty

      - name: Package IPA for TrollStore
        run: |
          mkdir -p build/Payload
          # Copy the .app bundle to Payload folder. 
          # Sử dụng cp -R thay vì cp -r cho an toàn trên MacOS
          cp -R build/Build/Products/Release-iphoneos/*.app build/Payload/
          cd build
          zip -qr MeoPhim_TrollStore_v${{ github.run_number }}.ipa Payload

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MeoPhim-Build-v${{ github.run_number }}
          path: build/MeoPhim_TrollStore_v${{ github.run_number }}.ipa
          retention-days: 7
